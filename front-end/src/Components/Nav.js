import React from 'react';

const Nav = (props) => {

    return (
        <header className="nav-header">
            <nav className="nav">
                
            </nav>
        </header>
    );
};

export default Nav;